apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pihole
    chart: pihole-2.31.0
    component: ""
    heritage: Helm
    release: pihole
  name: pihole
  namespace: dns
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
