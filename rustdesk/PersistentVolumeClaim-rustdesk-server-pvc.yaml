apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rustdesk-server-pvc
  namespace: rustdesk
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: longhorn-nobackup
