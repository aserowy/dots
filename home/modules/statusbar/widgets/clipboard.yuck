(defwidget clipboard []
    (eventbox
        :onclick "hyprctl dispatch exec ~/.config/clipman/open_history.nu"
        (label
            :class "nerd-icon"
            :text "󱉫"
        )
    )
)
