apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zigbee-tls-certificate
  namespace: homeassistant
spec:
  dnsNames:
    - zigbee.anderwerse.de
  duration: 2160h
  issuerRef:
    kind: ClusterIssuer
    name: azure-acme-issuer
  renewBefore: 720h
  secretName: zigbee-tls-certificate
