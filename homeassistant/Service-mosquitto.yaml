apiVersion: v1
kind: Service
metadata:
  name: mosquitto
  namespace: homeassistant
spec:
  ports:
    - name: http
      port: 1883
      protocol: TCP
  selector:
    app.kubernetes.io/name: mosquitto
