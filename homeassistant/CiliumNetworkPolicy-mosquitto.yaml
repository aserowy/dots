apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: mosquitto
  namespace: homeassistant
spec:
  egress:
    - {}
  endpointSelector:
    matchLabels:
      app: mosquitto
  ingress:
    - fromEndpoints:
        - matchLabels:
            app: zigbee2mqtt
      toPorts:
        - ports:
            - port: "1883"
              protocol: TCP
