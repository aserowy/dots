apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-tls-certificate
  namespace: argocd
spec:
  dnsNames:
    - argo.cluster.anderwerse.de
  duration: 2160h
  issuerRef:
    kind: ClusterIssuer
    name: azure-acme-issuer
  renewBefore: 720h
  secretName: argo-tls-certificate
