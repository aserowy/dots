apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: longhorn-volume-snapshot
  namespace: longhorn-system
spec:
  concurrency: 1
  cron: 0 0 * * *
  groups:
    - backup
  name: longhorn-volume-snapshot
  retain: 4
  task: snapshot
