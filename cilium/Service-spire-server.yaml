apiVersion: v1
kind: Service
metadata:
  name: spire-server
  namespace: cilium-spire
spec:
  ports:
    - name: grpc
      port: 8081
      protocol: TCP
      targetPort: grpc
  selector:
    app: spire-server
  type: ClusterIP
