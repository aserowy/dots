apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: adguard-tls-certificate
  namespace: adguard
spec:
  dnsNames:
    - adguard.anderwerse.de
  duration: 2160h
  issuerRef:
    kind: ClusterIssuer
    name: azure-acme-issuer
  renewBefore: 720h
  secretName: adguard-tls-certificate
